```swift
/*
 * UIKit 框架
 * Cocoa Touch UIKit
 * Cocoa 是苹果公司为 Mac OS X 所创建的原生面向对象的 API
 *
 * MVC Model -> Model View Controller
 * 模型(model)-视图(view)-控制器(controller)
 * Model(模型)：应用程序中用于处理应用程序数据逻辑的部分。->数据
            - 通常模型对象负责在数据库中存取数据
 * View(视图)：应用程序中处理数据显示的部分。           ->界面
            - 通常视图是依据模型数据创建的
 * Controller(控制器)：应用程序中处理用户交互的部分。    ->逻辑
            - 通常控制器负责从视图读取数据，控制用户输入，并向模型发送数据
 */
```